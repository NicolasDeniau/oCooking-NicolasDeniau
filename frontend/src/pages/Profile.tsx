import { useDocumentTitle } from "../hooks/useDocumentTitle";

const Profile = () => {
    useDocumentTitle("Profil");

    return (
        <>
            <h1>Profil</h1>
        </>
    )
};

export default Profile;