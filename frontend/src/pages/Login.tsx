import { useDocumentTitle } from "../hooks/useDocumentTitle";

const Login = () => {
    useDocumentTitle("Connexion");

    return (
        <>
            <h1>Connexion</h1>
        </>
    )
};

export default Login;